import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    historyTitle: {
        marginLeft: 3,
        marginBottom: 15,
        fontWeight: "700"
    },
});

export default styles;
